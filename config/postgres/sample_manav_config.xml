<?xml version="1.0"?>
<parameters>

    <!-- Connection details -->
    <type>POSTGRES</type>
    <driver>org.postgresql.Driver</driver>
    <url>jdbc:postgresql://localhost:5432/benchbase</url>
    <username>replicant</username>
    <password>replicant</password>
    <reconnectOnConnectionFailure>true</reconnectOnConnectionFailure>
    <isolation>TRANSACTION_READ_COMMITTED</isolation>
    <batchsize>128</batchsize>

    <!-- Scale factor affects number of initial log entries (default: 10,000) -->
    <scalefactor>1</scalefactor>

    <!-- The workload -->
    <terminals>2</terminals>
    <works>
        <work>
            <time>30</time>
            <rate>unlimited</rate>
            <!-- Only one transaction type with weight 100 -->
            <weights>100</weights>
        </work>
    </works>

    <!-- Manav Procedures declaration -->
    <transactiontypes>
        <transactiontype>
            <name>InsertRecord</name>
        </transactiontype>
    </transactiontypes>
</parameters> 