<?xml version="1.0"?>
<parameters>

    <!-- Connection details -->
    <type>SQLITE</type>
    <driver>org.sqlite.JDBC</driver>
    <url>jdbc:sqlite:twitter.db</url>
    <isolation>TRANSACTION_SERIALIZABLE</isolation>
    <batchsize>1024</batchsize>

    <!-- Scalefactor in Twitter scales by *100 the number of users-->
    <scalefactor>0.1</scalefactor>

    <!-- SQLITE only supports one writer thread -->
    <loaderThreads>1</loaderThreads>

    <!-- The workload -->
    <terminals>1</terminals>
    <works>
        <work>
            <time>60</time>
            <rate>10000</rate>
            <weights>1,1,7,90,1</weights>
        </work>
    </works>

    <!-- Twitter Procedures declaration -->
    <transactiontypes>
        <transactiontype>
            <name>GetTweet</name>
        </transactiontype>
        <transactiontype>
            <name>GetTweetsFromFollowing</name>
        </transactiontype>
        <transactiontype>
            <name>GetFollowers</name>
        </transactiontype>
        <transactiontype>
            <name>GetUserTweets</name>
        </transactiontype>
        <transactiontype>
            <name>InsertTweet</name>
        </transactiontype>
    </transactiontypes>
</parameters>
