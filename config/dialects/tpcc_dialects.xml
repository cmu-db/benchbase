<?xml version="1.0"?>
<dialects>
	<dialect driver="com.mysql.jdbc.Driver">
		<!-- Delivery Statements -->
		<stmt name="delivGetOrderIdSQL">
		SELECT no_o_id 
				FROM new_order 
				WHERE no_d_id = ? AND no_w_id = ? ORDER BY no_o_id ASC
		</stmt>
		<stmt name="delivDeleteNewOrderSQL">
		DELETE FROM new_order 
				WHERE no_o_id = ? AND no_d_id = ? AND no_w_id = ?
		</stmt>
		<stmt name="delivGetCustIdSQL">
		SELECT o_c_id 
				FROM oorder
				WHERE o_id = ? AND o_d_id = ? AND o_w_id = ?
		</stmt>
		<stmt name="delivUpdateCarrierIdSQL">
		UPDATE oorder SET o_carrier_id = ?
				WHERE o_id = ? AND o_d_id = ? AND o_w_id = ?
		</stmt>
		<stmt name="delivUpdateDeliveryDateSQL">
		UPDATE order_line SET ol_delivery_d = ? 
				WHERE ol_o_id = ? AND ol_d_id = ? AND ol_w_id = ?
		</stmt>
		<stmt name="delivSumOrderAmountSQL">
		SELECT SUM(ol_amount) AS ol_total 
				FROM order_line
				WHERE ol_o_id = ? AND ol_d_id = ? AND ol_w_id = ?
		</stmt>
		<stmt name="delivUpdateCustBalDelivCntSQL">
		UPDATE customer SET c_balance = c_balance + ?, c_delivery_cnt = c_delivery_cnt + 1
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
				
		<!-- New Order Statements -->
	    <stmt name="stmtGetCustWhseSQL">
	    SELECT c_discount, c_last, c_credit, w_tax 
	    		FROM customer, warehouse 
	    		WHERE w_id = ? AND c_w_id = ? AND c_d_id = ? AND c_id = ?
	    </stmt> 
	    
	    <stmt name="stmtGetDistSQL">
	    SELECT d_next_o_id, d_tax 
	    		FROM district
				WHERE d_w_id = ? AND d_id = ? FOR UPDATE
		</stmt>  
	    
		<stmt name="stmtInsertNewOrderSQL">
		INSERT INTO new_order (no_o_id, no_d_id, no_w_id) VALUES ( ?, ?, ?)
		</stmt>		
		<stmt name="stmtUpdateDistSQL">
		UPDATE district SET d_next_o_id = d_next_o_id + 1 
				WHERE d_w_id = ? AND d_id = ?
		</stmt>
		
		<stmt name="stmtInsertOOrderSQL">
		INSERT INTO oorder (o_id, o_d_id, o_w_id, o_c_id, o_entry_d, o_ol_cnt, o_all_local)
					VALUES (?, ?, ?, ?, ?, ?, ?)
		</stmt>
		
		<stmt name="stmtGetItemSQL">
		SELECT i_price, i_name , i_data 
				FROM item 
				WHERE i_id = ?
		</stmt>
	
		<stmt name="stmtGetStockSQL">
		SELECT s_quantity, s_data, s_dist_01, s_dist_02, s_dist_03, s_dist_04, s_dist_05, 
			   s_dist_06, s_dist_07, s_dist_08, s_dist_09, s_dist_10 
			   FROM stock 
			   WHERE s_i_id = ? AND s_w_id = ? FOR UPDATE
		</stmt>
		
		<stmt name="stmtUpdateStockSQL">
		UPDATE stock SET s_quantity = ? , s_ytd = s_ytd + ?, s_remote_cnt = s_remote_cnt + ?
						WHERE s_i_id = ? AND s_w_id = ?
		</stmt>
		
		<stmt name="stmtInsertOrderLineSQL">
		INSERT INTO order_line (ol_o_id, ol_d_id, ol_w_id, ol_number, ol_i_id, ol_supply_w_id, ol_quantity, ol_amount, ol_dist_info) 
						VALUES (?,?,?,?,?,?,?,?,?)
		</stmt>
		
		<!-- OrderStatus Statements -->
		<stmt name="ordStatGetNewestOrdSQL">
		SELECT o_id, o_carrier_id, o_entry_d 
				FROM oorder
				WHERE o_w_id = ? AND o_d_id = ? AND o_c_id = ? ORDER BY o_id DESC LIMIT 1
		</stmt>
		
		<stmt name="ordStatGetOrderLinesSQL">
		SELECT ol_i_id, ol_supply_w_id, ol_quantity, ol_amount, ol_delivery_d FROM order_line
				WHERE ol_o_id = ? AND ol_d_id =? AND ol_w_id = ?
		</stmt>
		
		<stmt name="payGetCustSQL">
		SELECT c_first, c_middle, c_last, c_street_1, c_street_2, 
				c_city, c_state, c_zip, c_phone, c_credit, c_credit_lim, 
				c_discount, c_balance, c_ytd_payment, c_payment_cnt, c_since FROM customer 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		
		<stmt name="customerByNameSQL">
				SELECT c_first, c_middle, c_id, c_street_1, c_street_2, c_city, 
				c_state, c_zip, c_phone, c_credit, c_credit_lim, c_discount, 
				c_balance, c_ytd_payment, c_payment_cnt, c_since FROM customer 
				WHERE c_w_id = ? AND c_d_id = ? AND c_last = ? ORDER BY c_first
		</stmt>
		
		<!-- Payment Statments -->
		<stmt name="payUpdateWhseSQL">
			UPDATE warehouse SET w_ytd = w_ytd + ?  WHERE w_id = ?
		</stmt>
		<stmt name="payGetWhseSQL">
		SELECT w_street_1, w_street_2, w_city, w_state, w_zip, w_name
				 FROM warehouse 
				 WHERE w_id = ?
		</stmt>
		<stmt name="payUpdateDistSQL">
		UPDATE district SET d_ytd = d_ytd + ? 
				WHERE d_w_id = ? AND d_id = ?
		</stmt>
		<stmt name="payGetDistSQL">
		SELECT d_street_1, d_street_2, d_city, d_state, d_zip, d_name
				 FROM district 
				 WHERE d_w_id = ? AND d_id = ?
		</stmt>
		<stmt name="payGetCustSQL">
		SELECT c_first, c_middle, c_last, c_street_1, c_street_2,
				c_city, c_state, c_zip, c_phone, c_credit, c_credit_lim,
				c_discount, c_balance, c_ytd_payment, c_payment_cnt, c_since FROM customer 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		<stmt name="payGetCustCdataSQL">
		SELECT c_data 
				FROM customer 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		<stmt name="payUpdateCustBalCdataSQL">
		UPDATE customer SET c_balance = ?, c_ytd_payment = ?, c_payment_cnt = ?, c_data = ? 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		<stmt name="payUpdateCustBalSQL">
		UPDATE customer SET c_balance = ?, c_ytd_payment = ?, c_payment_cnt = ? 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		<stmt name="payInsertHistSQL">
		INSERT INTO history (h_c_d_id, h_c_w_id, h_c_id, h_d_id, h_w_id, h_date, h_amount, h_data)
				 VALUES (?,?,?,?,?,?,?,?)
		</stmt>
		<stmt name="customerByNameSQL">
		SELECT  c_first, c_middle, c_id, c_street_1, c_street_2, c_city,
				c_state, c_zip, c_phone, c_credit, c_credit_lim, c_discount,
				c_balance, c_ytd_payment, c_payment_cnt, c_since 
				FROM customer
				WHERE c_w_id = ? AND c_d_id = ? AND c_last = ? ORDER BY c_first
		</stmt>
		<!-- Stock Level Statements -->
		<stmt name="stockGetDistOrderIdSQL">
		SELECT d_next_o_id
				FROM district
				WHERE d_w_id = ? AND d_id = ?</stmt>
		
		<stmt name="stockGetCountStockSQL">
		SELECT COUNT(DISTINCT (s_i_id)) AS stock_count
				FROM order_line, stock
				WHERE ol_w_id = ? AND ol_d_id = ? AND ol_o_id &lt; ?
				AND ol_o_id >= ? - 20 AND s_w_id = ?
				AND s_i_id = ol_i_id AND s_quantity &lt; ?
		</stmt>
	</dialect>
	
	
	
	<dialect engine="oracle.jdbc.driver.OracleDriver">
		<!-- Delivery Statements -->
		<stmt name="delivGetOrderIdSQL">
		SELECT no_o_id 
				FROM new_order 
				WHERE no_d_id = ? AND no_w_id = ? AND ROWNUM = 1 ORDER BY no_o_id ASC
		</stmt>
		<stmt name="delivDeleteNewOrderSQL">
		DELETE FROM new_order 
				WHERE no_o_id = ? AND no_d_id = ? AND no_w_id = ?
		</stmt>
		<stmt name="delivGetCustIdSQL">
		SELECT o_c_id 
				FROM oorder
				WHERE o_id = ? AND o_d_id = ? AND o_w_id = ?
		</stmt>
		<stmt name="delivUpdateCarrierIdSQL">
		UPDATE oorder SET o_carrier_id = ?
				WHERE o_id = ? AND o_d_id = ? AND o_w_id = ?
		</stmt>
		<stmt name="delivUpdateDeliveryDateSQL">
		UPDATE order_line SET ol_delivery_d = ? 
				WHERE ol_o_id = ? AND ol_d_id = ? AND ol_w_id = ?
		</stmt>
		<stmt name="delivSumOrderAmountSQL">
		SELECT SUM(ol_amount) AS ol_total 
				FROM order_line
				WHERE ol_o_id = ? AND ol_d_id = ? AND ol_w_id = ?
		</stmt>
		<stmt name="delivUpdateCustBalDelivCntSQL">
		UPDATE customer SET c_balance = c_balance + ? , c_delivery_cnt = c_delivery_cnt + 1
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
				
		<!-- New Order Statements -->
	    <stmt name="stmtGetCustWhseSQL">
	    SELECT c_discount,c_last,c_credit,w_tax 
	    		FROM customer, warehouse 
	    		WHERE w_id = ? AND c_w_id = ? AND c_d_id = ? AND c_id = ?
	    </stmt> 
	    
	    <stmt name="stmtGetDistSQL">
	    SELECT d_next_o_id,d_tax 
	    		FROM district
				WHERE d_w_id = ? AND d_id = ?
		</stmt>  
	    
		<stmt name="stmtInsertNewOrderSQL">
		INSERT INTO new_order (no_o_id,no_d_id, no_w_id) VALUES (?,?,?)
		</stmt>		
		<stmt name="stmtUpdateDistSQL">
		UPDATE district SET d_next_o_id = d_next_o_id + 1 
				WHERE d_w_id = ? AND d_id = ?
		</stmt>
		
		<stmt name="stmtInsertOOrderSQL">
		INSERT INTO oorder (o_id, o_d_id, o_w_id, o_c_id, o_entry_d, o_ol_cnt, o_all_local)
					VALUES (?, ?, ?, ?, ?, ?, ?)
		</stmt>
		
		<stmt name="stmtGetItemSQL">
		SELECT i_price, i_name , i_data 
				FROM item 
				WHERE i_id = ?
		</stmt>
	
		<stmt name="stmtGetStockSQL">
		SELECT s_quantity, s_data, s_dist_01, s_dist_02, s_dist_03, s_dist_04, s_dist_05, 
			   s_dist_06, s_dist_07, s_dist_08, s_dist_09, s_dist_10 
			   FROM stock 
			   WHERE s_i_id = ? AND s_w_id = ?
		</stmt>
		
		<stmt name="stmtUpdateStockSQL">
		UPDATE stock SET s_quantity = ? , s_ytd = s_ytd + ?, s_remote_cnt = s_remote_cnt + ?
						WHERE s_i_id = ? AND s_w_id = ?
		</stmt>
		
		<stmt name="stmtInsertOrderLineSQL">
		INSERT INTO order_line (ol_o_id, ol_d_id, ol_w_id, ol_number, ol_i_id, ol_supply_w_id, ol_quantity, ol_amount, ol_dist_info) 
						VALUES (?,?,?,?,?,?,?,?,?)
		</stmt>
		
		<!-- OrderStatus Statements -->
		<stmt name="ordStatGetNewestOrdSQL">
		SELECT o_id, o_carrier_id, o_entry_d 
				FROM oorder
				WHERE o_w_id = ? AND o_d_id = ? AND o_c_id = ? AND ROWNUM =1 ORDER BY o_id DESC
		</stmt>
		
		<stmt name="ordStatGetOrderLinesSQL">
		SELECT ol_i_id, ol_supply_w_id, ol_quantity, ol_amount, ol_delivery_d FROM order_line
				WHERE ol_o_id = ? AND ol_d_id =? AND ol_w_id = ?
		</stmt>
		
		<stmt name="payGetCustSQL">
		SELECT c_first, c_middle, c_last, c_street_1, c_street_2, 
				c_city, c_state, c_zip, c_phone, c_credit, c_credit_lim, 
				c_discount, c_balance, c_ytd_payment, c_payment_cnt, c_since FROM customer 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		
		<stmt name="customerByNameSQL">
				SELECT c_first, c_middle, c_id, c_street_1, c_street_2, c_city, 
				c_state, c_zip, c_phone, c_credit, c_credit_lim, c_discount, 
				c_balance, c_ytd_payment, c_payment_cnt, c_since FROM customer 
				WHERE c_w_id = ? AND c_d_id = ? AND c_last = ? ORDER BY c_first
		</stmt>
		
		<!-- Payment Statments -->
		<stmt name="payUpdateWhseSQL">
			UPDATE warehouse SET w_ytd = w_ytd + ?  WHERE w_id = ?
		</stmt>
		<stmt name="payGetWhseSQL">
		SELECT w_street_1, w_street_2, w_city, w_state, w_zip, w_name
				 FROM warehouse 
				 WHERE w_id = ?
		</stmt>
		<stmt name="payUpdateDistSQL">
		UPDATE district SET d_ytd = d_ytd + ? 
				WHERE d_w_id = ? AND d_id = ?
		</stmt>
		<stmt name="payGetDistSQL">
		SELECT d_street_1, d_street_2, d_city, d_state, d_zip, d_name
				 FROM district 
				 WHERE d_w_id = ? AND d_id = ?
		</stmt>
		<stmt name="payGetCustSQL">
		SELECT c_first, c_middle, c_last, c_street_1, c_street_2,
				c_city, c_state, c_zip, c_phone, c_credit, c_credit_lim,
				c_discount, c_balance, c_ytd_payment, c_payment_cnt, c_since FROM customer 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		<stmt name="payGetCustCdataSQL">
		SELECT c_data 
				FROM customer 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		<stmt name="payUpdateCustBalCdataSQL">
		UPDATE customer SET c_balance = ?, c_ytd_payment = ?, c_payment_cnt = ?, c_data = ? 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		<stmt name="payUpdateCustBalSQL">
		UPDATE customer SET c_balance = ?, c_ytd_payment = ?, c_payment_cnt = ? 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		<stmt name="payInsertHistSQL">
		INSERT INTO history (h_c_d_id, h_c_w_id, h_c_id, h_d_id, h_w_id, h_date, h_amount, h_data)
				 VALUES (?,?,?,?,?,?,?,?)
		</stmt>
		<stmt name="customerByNameSQL">
		SELECT  c_first, c_middle, c_id, c_street_1, c_street_2, c_city,
				c_state, c_zip, c_phone, c_credit, c_credit_lim, c_discount,
				c_balance, c_ytd_payment, c_payment_cnt, c_since 
				FROM customer
				WHERE c_w_id = ? AND c_d_id = ? AND c_last = ? ORDER BY c_first
		</stmt>
		<!-- Stock Level Statements -->
		<stmt name="stockGetDistOrderIdSQL">
		SELECT d_next_o_id
				FROM district
				WHERE d_w_id = ? AND d_id = ?</stmt>
		
		<stmt name="stockGetCountStockSQL">
		SELECT COUNT(DISTINCT (s_i_id)) AS stock_count
				FROM order_line, stock
				WHERE ol_w_id = ? AND ol_d_id = ? AND ol_o_id &lt; ?
				AND ol_o_id >= ? - 20 AND s_w_id = ?
				AND s_i_id = ol_i_id AND s_quantity &lt; ?
		</stmt>	
		
	<!-- MS SQL -->	
	</dialect>
		<dialect engine="com.microsoft.sqlserver.jdbc.SQLServerDriver">
		<!-- Delivery Statements -->
		<stmt name="delivGetOrderIdSQL">
		SELECT TOP 1 no_o_id 
				FROM new_order 
				WHERE no_d_id = ? AND no_w_id = ? ORDER BY no_o_id ASC
		</stmt>
		<stmt name="delivDeleteNewOrderSQL">
		DELETE FROM new_order 
				WHERE no_o_id = ? AND no_d_id = ? AND no_w_id = ?
		</stmt>
		<stmt name="delivGetCustIdSQL">
		SELECT o_c_id 
				FROM oorder
				WHERE o_id = ? AND o_d_id = ? AND o_w_id = ?
		</stmt>
		<stmt name="delivUpdateCarrierIdSQL">
		UPDATE oorder SET o_carrier_id = ?
				WHERE o_id = ? AND o_d_id = ? AND o_w_id = ?
		</stmt>
		<stmt name="delivUpdateDeliveryDateSQL">
		UPDATE order_line SET ol_delivery_d = ? 
				WHERE ol_o_id = ? AND ol_d_id = ? AND ol_w_id = ?
		</stmt>
		<stmt name="delivSumOrderAmountSQL">
		SELECT SUM(ol_amount) AS ol_total 
				FROM order_line
				WHERE ol_o_id = ? AND ol_d_id = ? AND ol_w_id = ?
		</stmt>
		<stmt name="delivUpdateCustBalDelivCntSQL">
		UPDATE customer SET c_balance = c_balance + ? , c_delivery_cnt = c_delivery_cnt + 1
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
				
		<!-- New Order Statements -->
	    <stmt name="stmtGetCustWhseSQL">
	    SELECT c_discount,c_last,c_credit,w_tax 
	    		FROM customer, warehouse 
	    		WHERE w_id = ? AND c_w_id = ? AND c_d_id = ? AND c_id = ?
	    </stmt> 
	    
	    <stmt name="stmtGetDistSQL">
	    SELECT d_next_o_id,d_tax 
	    		FROM district
				WHERE d_w_id = ? AND d_id = ?
		</stmt>  
	    
		<stmt name="stmtInsertNewOrderSQL">
		INSERT INTO new_order (no_o_id,no_d_id, no_w_id) VALUES (?,?,?)
		</stmt>		
		<stmt name="stmtUpdateDistSQL">
		UPDATE district SET d_next_o_id = d_next_o_id + 1 
				WHERE d_w_id = ? AND d_id = ?
		</stmt>
		
		<stmt name="stmtInsertOOrderSQL">
		INSERT INTO oorder (o_id, o_d_id, o_w_id, o_c_id, o_entry_d, o_ol_cnt, o_all_local)
					VALUES (?, ?, ?, ?, ?, ?, ?)
		</stmt>
		
		<stmt name="stmtGetItemSQL">
		SELECT i_price, i_name , i_data 
				FROM item 
				WHERE i_id = ?
		</stmt>
	
		<stmt name="stmtGetStockSQL">
		SELECT s_quantity, s_data, s_dist_01, s_dist_02, s_dist_03, s_dist_04, s_dist_05, 
			   s_dist_06, s_dist_07, s_dist_08, s_dist_09, s_dist_10 
			   FROM stock 
			   WHERE s_i_id = ? AND s_w_id = ?
		</stmt>
		
		<stmt name="stmtUpdateStockSQL">
		UPDATE stock SET s_quantity = ? , s_ytd = s_ytd + ?, s_remote_cnt = s_remote_cnt + ?
						WHERE s_i_id = ? AND s_w_id = ?
		</stmt>
		
		<stmt name="stmtInsertOrderLineSQL">
		INSERT INTO order_line (ol_o_id, ol_d_id, ol_w_id, ol_number, ol_i_id, ol_supply_w_id, ol_quantity, ol_amount, ol_dist_info) 
						VALUES (?,?,?,?,?,?,?,?,?)
		</stmt>
		
		<!-- OrderStatus Statements -->
		<stmt name="ordStatGetNewestOrdSQL">
		SELECT TOP 1 o_id, o_carrier_id, o_entry_d 
				FROM oorder
				WHERE o_w_id = ? AND o_d_id = ? AND o_c_id = ? ORDER BY o_id DESC
		</stmt>
		
		<stmt name="ordStatGetOrderLinesSQL">
		SELECT ol_i_id, ol_supply_w_id, ol_quantity, ol_amount, ol_delivery_d FROM order_line
				WHERE ol_o_id = ? AND ol_d_id =? AND ol_w_id = ?
		</stmt>
		
		<stmt name="payGetCustSQL">
		SELECT c_first, c_middle, c_last, c_street_1, c_street_2, 
				c_city, c_state, c_zip, c_phone, c_credit, c_credit_lim, 
				c_discount, c_balance, c_ytd_payment, c_payment_cnt, c_since FROM customer 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		
		<stmt name="customerByNameSQL">
				SELECT c_first, c_middle, c_id, c_street_1, c_street_2, c_city, 
				c_state, c_zip, c_phone, c_credit, c_credit_lim, c_discount, 
				c_balance, c_ytd_payment, c_payment_cnt, c_since FROM customer 
				WHERE c_w_id = ? AND c_d_id = ? AND c_last = ? ORDER BY c_first
		</stmt>
		
		<!-- Payment Statments -->
		<stmt name="payUpdateWhseSQL">
			UPDATE warehouse SET w_ytd = w_ytd + ?  WHERE w_id = ?
		</stmt>
		<stmt name="payGetWhseSQL">
		SELECT w_street_1, w_street_2, w_city, w_state, w_zip, w_name
				 FROM warehouse 
				 WHERE w_id = ?
		</stmt>
		<stmt name="payUpdateDistSQL">
		UPDATE district SET d_ytd = d_ytd + ? 
				WHERE d_w_id = ? AND d_id = ?
		</stmt>
		<stmt name="payGetDistSQL">
		SELECT d_street_1, d_street_2, d_city, d_state, d_zip, d_name
				 FROM district 
				 WHERE d_w_id = ? AND d_id = ?
		</stmt>
		<stmt name="payGetCustSQL">
		SELECT c_first, c_middle, c_last, c_street_1, c_street_2,
				c_city, c_state, c_zip, c_phone, c_credit, c_credit_lim,
				c_discount, c_balance, c_ytd_payment, c_payment_cnt, c_since FROM customer 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		<stmt name="payGetCustCdataSQL">
		SELECT c_data 
				FROM customer 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		<stmt name="payUpdateCustBalCdataSQL">
		UPDATE customer SET c_balance = ?, c_ytd_payment = ?, c_payment_cnt = ?, c_data = ? 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		<stmt name="payUpdateCustBalSQL">
		UPDATE customer SET c_balance = ?, c_ytd_payment = ?, c_payment_cnt = ? 
				WHERE c_w_id = ? AND c_d_id = ? AND c_id = ?
		</stmt>
		<stmt name="payInsertHistSQL">
		INSERT INTO history (h_c_d_id, h_c_w_id, h_c_id, h_d_id, h_w_id, h_date, h_amount, h_data)
				 VALUES (?,?,?,?,?,?,?,?)
		</stmt>
		<stmt name="customerByNameSQL">
		SELECT  c_first, c_middle, c_id, c_street_1, c_street_2, c_city,
				c_state, c_zip, c_phone, c_credit, c_credit_lim, c_discount,
				c_balance, c_ytd_payment, c_payment_cnt, c_since 
				FROM customer
				WHERE c_w_id = ? AND c_d_id = ? AND c_last = ? ORDER BY c_first
		</stmt>
		<!-- Stock Level Statements -->
		<stmt name="stockGetDistOrderIdSQL">
		SELECT d_next_o_id
				FROM district
				WHERE d_w_id = ? AND d_id = ?</stmt>
		
		<stmt name="stockGetCountStockSQL">
		SELECT COUNT(DISTINCT (s_i_id)) AS stock_count
				FROM order_line, stock
				WHERE ol_w_id = ? AND ol_d_id = ? AND ol_o_id &lt; ?
				AND ol_o_id >= ? - 20 AND s_w_id = ?
				AND s_i_id = ol_i_id AND s_quantity &lt; ?
		</stmt>	
	</dialect>
</dialects>
